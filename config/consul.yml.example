default: &default
  smtp:
    domain:               chiloeches-consul.dev.aspgems.com # the domain your emails will come from
    address:              localhost  # SMTP server used to send emails
    port:                 25
    authentication:       plain
    enable_starttls_auto: false

development:
  <<: *default

test:
  <<: *default

production: &production
